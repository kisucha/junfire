| 항목 | 내용 |
|------|------|
| Document Name | JunFire 반복실수 기록 |
| Version | V1 |
| Date | 2026-06-16 |
| Author | Claude Sonnet 4.6 |
| Document Type | 실수 방지 체크리스트 |
| Model Used | claude-sonnet-4-6 |

---

## 실수 #1 — 기존 파일 파악 없이 중복 구현

| 항목 | 내용 |
|------|------|
| 실수 유형 | 이미 구현된 파일을 모르고 중복 생성 → 충돌/빌드 오류 |
| 날짜 | 2026-06-16 |
| 구체적 사례 | `src/proxy.ts` (isFirstLogin 로직 + Role 분기 완성) 존재했는데 `src/middleware.ts` 새로 생성 → Next.js 16 빌드 오류. `change-password/page.tsx`, `/api/password/route.ts`도 이미 존재했음. |
| 근본 원인 | 구현 시작 전 코드베이스 전체 탐색 생략. 요청받은 기능 파일만 일부 확인하고 바로 구현 진입. |

### 방지 절차 — 구현 전 반드시 실행

```
[ ] 1. 전체 파일 목록 탐색
        Glob: src/**/*.ts, src/**/*.tsx

[ ] 2. Next.js 특수 파일 우선 확인
        middleware.ts / proxy.ts / layout.tsx 존재 여부

[ ] 3. 요청 기능과 관련된 API 라우트 전수 확인
        Glob: src/app/api/**/*.ts

[ ] 4. 요청 기능과 관련된 페이지 전수 확인
        Glob: src/app/**/page.tsx

[ ] 5. 위 탐색 결과를 계획서에 명시 후 사용자 확인
        "기존 파일 X가 있습니다. Y를 수정합니다. Z는 신규 생성합니다."
```

### 규칙

- 신규 파일 생성 전: 동일 목적 파일이 이미 있는지 반드시 Grep/Glob으로 확인
- Next.js 특수 파일(`middleware.ts`, `proxy.ts`)은 프레임워크 버전 차이로 이름이 다를 수 있음 → 양쪽 모두 확인
- API 엔드포인트 생성 전: 동일 경로/유사 경로 라우트 파일 존재 여부 확인

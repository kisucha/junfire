# Critic Review — RESEARCH.md V1

| 항목 | 내용 |
|------|------|
| Document Name | RESEARCH.md 비판적 검토 보고서 |
| Version | V1 |
| Date | 2026-05-18 |
| Author | critic (claude-sonnet-4-6) |
| Document Type | 비판적 검토 보고서 |
| Model Used | claude-sonnet-4-6 |

---

## 심각도 분류

- CRITICAL: 반드시 수정 (기능 불가능하거나 보안 문제)
- MAJOR: 수정 필요 (모호함, 누락, 잘못된 정의)
- MINOR: 권장 개선 (더 명확하게 할 수 있음)

---

## 발견된 문제 목록

---

### 1. 야간 근무 데이터 모델이 근본적으로 깨짐 — CRITICAL

- **위치:** RESEARCH.md 섹션 5.2 (WorkRecord 엔티티), 섹션 7 (BR-002), 섹션 9 (EC-004)
- **문제:**
  startTime, endTime을 `String (HH:mm)` 타입으로 저장하는 방식을 제안하면서,
  동시에 BR-002에서 `endTime > startTime` 규칙을 적용하고 있다.
  야간 근무(예: 22:00 ~ 02:00)는 endTime(02:00) < startTime(22:00) 이므로 이 규칙을 위반한다.
  EC-004에서 "자정 초과 감지 로직 필요"라고만 적고 처리 방안을 미확정으로 남겼다.
  그러나 "자정 초과 케이스 처리"는 단순한 예외 로직이 아니라, 날짜(date)와 시간(time) 저장 구조 자체를 바꿔야 할 수 있는 데이터 모델 결정 사항이다.
  선택지가 명확히 제시되지 않았다: (a) endDate 컬럼 추가, (b) ISO DateTime으로 저장, (c) 자정 초과 허용 시 totalHours 수동 입력 허용, (d) 야간 근무 자체를 두 개 레코드로 분리.
  어떤 방향이든 데이터 모델이 달라지므로 UC-004에 단순히 "미확정"으로 방치하는 것은 설계 미완성이다.
- **영향:**
  소방 업종 특성상 야간 출동은 현실적으로 발생 가능성이 높다.
  데이터 모델 미결정 상태에서 구현을 시작하면, 야간 근무 기록 시 totalHours 계산이 음수(-20시간)로 저장되거나 에러가 발생하며, 이후 모델 변경 시 마이그레이션이 필요하다.
- **개선 방향:**
  UC-004를 "미확정"으로 방치하지 말고 반드시 결론을 내려야 한다.
  가장 단순한 안은 `endTime < startTime`일 경우 endTime에 +24시간을 적용해 totalHours를 계산하고, `isOvernightWork Boolean` 플래그를 추가하는 것이다.
  이를 RESEARCH.md에 명시적 결정사항으로 올려야 한다.

---

### 2. HOLIDAY 상태가 WorkRecord에 직원별로 저장되는 구조적 결함 — CRITICAL

- **위치:** RESEARCH.md 섹션 5.2 (Status Enum), 섹션 3.4 (HOLIDAY 처리), 섹션 7 (BR-004)
- **문제:**
  현재 데이터 모델에서 HOLIDAY는 WorkRecord의 status 값 중 하나로 저장된다.
  즉, 2026-05-05(어린이날)가 공휴일이면, 직원 5명 각자에게 WorkRecord(status=HOLIDAY)가 5개 생성되어야 한다.
  그러나 RESEARCH.md 어디에도 "관리자가 공휴일을 지정하면 모든 직원에게 자동으로 HOLIDAY 레코드가 생성된다"는 로직이 명시되지 않았다.
  UC-007에서 공휴일 지정 방식을 미확정으로 남겼고, "직원 개인별 자동 적용? 한국 공휴일 API 자동 연동?"이라고 나열만 했을 뿐 아무 결론이 없다.
  달력에서 "공휴일 표시"를 위해 DB를 조회하면, 레코드가 없는 날에는 공휴일이 표시되지 않는다. 결국 공휴일 정보를 어디에 어떻게 저장하는지가 결정되지 않으면 달력 렌더링 자체가 불가능하다.
  별도의 `Holiday` 테이블(date, name) 없이 WorkRecord에 HOLIDAY status를 넣는 방식은 공휴일 추가 시 N명 × 1일 레코드를 한꺼번에 INSERT해야 하는 비정규화 구조다.
- **영향:**
  달력 색상 표시(HOLIDAY = 빨간색) 구현 불가.
  관리자 "공휴일 지정" 기능 구현 방향 전혀 알 수 없음.
  직원이 공휴일에 실제로 근무했을 경우(WORK + HOLIDAY 중복) 처리 불가.
- **개선 방향:**
  별도 `Holiday` 테이블(id, date UNIQUE, name String, createdAt) 추가를 검토해야 한다.
  달력에서 공휴일은 `Holiday` 테이블에서 읽고, 직원 근무 기록은 WorkRecord에서 읽어 오버레이 표시하는 방식으로 분리해야 한다.
  "공휴일 날짜에 직원이 WORK 기록 가능 여부"는 RESEARCH.md에서 명시적 결정이 필요하다.

---

### 3. EMPLOYEE가 타인 기록에 URL 조작으로 접근 가능한 보안 공백 — CRITICAL

- **위치:** RESEARCH.md 섹션 3.1 (보안), 섹션 6.4 (기록 입력/수정), 섹션 7 (BR-008)
- **문제:**
  EC-008에서 "/record/:date 에서 userId 조작"에 대해 "URL에 userId 없음 (세션의 userId 사용). 서버에서 세션 userId로만 조회/저장"으로 처리한다고 되어 있다.
  그러나 관리자 대시보드(/admin) API들에 대한 권한 검증 명세가 매우 추상적이다.
  구체적으로 다음 API들의 서버사이드 권한 검증 방식이 전혀 명시되지 않았다:
  - GET /api/records?userId=xxx&startDate=yyy (관리자가 특정 직원 기록 조회)
  - GET /api/users (직원 목록 조회)
  - POST /api/users (직원 등록)
  - PUT /api/users/:id (직원 정보 수정)
  - POST /api/report/pdf (PDF 생성)
  즉, "ADMIN 전용 API에 반드시 Role 검사 적용"이라는 원칙만 있고, 각 API별로 구체적으로 무엇을 검증해야 하는지의 명세가 없다.
  또한 BR-009에서 "ADMIN 모든 직원 기록: 수정/삭제 권한 미명시 (확인 필요)"로 남아 있어, 구현자가 임의로 결정해야 하는 상황이다.
- **영향:**
  API 설계 단계에서 권한 누락이 발생할 수 있다. 특히 EMPLOYEE가 관리자 API를 직접 호출하는 공격(권한 상승)에 대한 방어 설계가 불완전하다.
- **개선 방향:**
  각 API 엔드포인트별로 "허용 Role"을 테이블로 명시해야 한다.
  BR-009 (ADMIN의 직원 기록 수정/삭제 권한)는 반드시 결론을 내려야 한다.

---

### 4. 비밀번호 정책 완전 부재 — CRITICAL

- **위치:** RESEARCH.md 섹션 3.1 (인증/보안), 섹션 6.2 (로그인)
- **문제:**
  비밀번호 해시 저장(bcrypt)은 명시되어 있으나, 비밀번호 정책(최소 길이, 복잡도, 초기 비밀번호 변경 강제 등)이 전혀 없다.
  특히 직원 등록 시 관리자가 초기 비밀번호를 설정하는데, 이 초기 비밀번호를 어떻게 직원에게 전달하는지, 최초 로그인 시 비밀번호 변경을 강제하는지 여부가 전혀 언급되지 않았다.
  로그인 실패 횟수 제한(Brute-force 방어)도 없다.
  /login 페이지 유효성 검사에서 "비밀번호: 최소 1자 이상(빈값 방지)"으로만 정의되어 있어, 1자짜리 비밀번호도 허용된다.
- **영향:**
  보안 수준이 현저히 낮다. 업무 기록과 급여 정보에 접근하는 시스템인데 비밀번호 "1234" 등이 허용된다.
  초기 비밀번호 전달 방식 미정으로 인해 운영 시 관리자가 어떻게 직원에게 계정 정보를 줄지 알 수 없다.
- **개선 방향:**
  비밀번호 최소 8자 이상, 초기 비밀번호 변경 강제(force change) 여부 정책을 명시해야 한다.
  로그인 실패 5회 이상 시 처리 방안(잠금 또는 대기 시간)도 정의해야 한다.

---

### 5. 동일 날짜 중복 기록 UNIQUE 제약이 야간 근무 케이스를 처리 못함 — CRITICAL

- **위치:** RESEARCH.md 섹션 5.2 (고유 제약 조건), 섹션 9 (EC-002, EC-004)
- **문제:**
  `(userId, date) 복합 UNIQUE` 제약이 있어 동일 직원이 같은 날 두 개의 기록을 남길 수 없다.
  그러나 소방 업종에서는 같은 날에 오전 현장 출동(09:00~12:00)과 오후 교육(14:00~17:00)처럼 두 개의 별개 업무가 발생할 수 있다.
  RESEARCH.md는 이 케이스를 전혀 고려하지 않았다.
  하루에 하나의 기록만 허용하는 것이 비즈니스 요구사항인지, 아니면 기술적 제약인지 명확한 결정 근거가 없다.
- **영향:**
  "하루 1건만 허용" 정책이 사용자 요구사항과 맞지 않을 경우, 데이터 모델을 처음부터 재설계해야 한다.
- **개선 방향:**
  CLAUDE.md에서 "업무 시간 기록"을 "하루 1건"으로 제한한다는 명시적 요구사항이 있는지 확인해야 한다.
  없다면, UNIQUE 제약 근거를 명시하거나 하루 복수 기록 허용 여부를 비즈니스 결정사항으로 올려야 한다.

---

### 6. PDF 레이아웃 상세 정보 심각하게 부족 — MAJOR

- **위치:** RESEARCH.md 섹션 3.6 (PDF 다운로드), 섹션 7 (PDF 포함 항목 규칙)
- **문제:**
  PDF 레이아웃이 3줄로만 요약되어 있다:
  "1. 표지: 회사명, 보고서 제목, 기간, 생성일
  2. 집계 요약: 직원별 근무시간/휴가 요약 테이블
  3. 상세 기록: 직원별 날짜별 업무 내용 (상태, 시간, 장소, 내용)"
  다음 항목들이 모두 미정이다:
  - 페이지 헤더/푸터에 무엇이 들어가는가 (페이지 번호, 회사명 반복 등)
  - 직원별 상세 기록이 한 페이지에 한 직원인가, 아니면 연속 출력인가
  - 날짜별 정렬 순서 (오름차순/내림차순)
  - 테이블 컬럼 순서 및 너비 비율
  - 기록이 없는 날짜(미기록)를 PDF에 포함하는가
  - SICK/ANNUAL/UNPAID 날짜의 업무 내용(description) null 시 "없음"으로 표시하는가 공백인가
  - 직원별 PDF 분리 출력 여부 (UC-011: 미확정으로만 나옴)
  - 집계 요약의 총 근무시간 단위 (8시간? 8.5시간? 8:30?) — UC-012도 미확정
  - A4 용지 세로 기준인지 가로 기준인지
- **영향:**
  PDF 구현 단계에서 구현자가 모든 레이아웃을 임의로 결정해야 하며, 결과물이 사용자 기대와 다를 가능성이 높다. PDF는 노무 신고·세무 처리용 증빙 자료로 활용되므로, 레이아웃 수정 요구가 구현 후에 발생할 확률이 매우 높다.
- **개선 방향:**
  와이어프레임 수준의 PDF 레이아웃 스케치가 필요하다. 최소한 각 섹션의 컬럼 정의와 정렬 기준을 표로 명시해야 한다. UC-011, UC-012는 반드시 결론을 내야 한다.

---

### 7. 관리자의 직원 기록 수정/삭제 권한이 미확정으로 방치됨 — MAJOR

- **위치:** RESEARCH.md 섹션 7 (BR-009), 섹션 11 (UC-002)
- **문제:**
  BR-009: "ADMIN 모든 직원 기록: 읽기 O, 수정/삭제 권한 미명시 (확인 필요)"
  UC-002: "ADMIN이 직원 기록 수정/삭제 가능 여부 — 미확정"
  이것은 단순한 UX 편의 문제가 아니다. 관리자가 기록을 수정할 수 없다면:
  - 직원이 실수로 잘못 입력한 기록을 바로잡을 방법이 없다 (직원 본인이 수정해야 함)
  - 직원이 퇴직 처리(isActive=false)된 후 과거 기록에 오류가 있어도 수정 불가
  반면 관리자가 기록을 수정할 수 있다면:
  - 수정 이력 관리가 필요해진다 (누가 언제 수정했는가)
  이 결정이 없으면 관리자 대시보드(/admin)의 UI 요소 자체가 달라진다.
- **영향:**
  관리자 대시보드 기능 범위가 완전히 달라지며, API 권한 설계에 직접적인 영향을 준다. 구현 후 정책 변경 시 API와 UI 모두 재작성 필요.
- **개선 방향:**
  반드시 결론을 내야 한다. 소규모 운영 특성상 "관리자가 직원 기록 수정 가능하되, 수정 일시와 수정자를 WorkRecord에 기록(modifiedBy, modifiedAt 컬럼)"하는 방향을 추천한다는 분석적 의견이라도 RESEARCH.md에 담겨야 한다.

---

### 8. ADMIN이 /dashboard 접근 시 처리 방식 미확정이 미들웨어 설계를 막음 — MAJOR

- **위치:** RESEARCH.md 섹션 3.3 (접근 제어), 섹션 6.3, 섹션 9 (EC-007), 섹션 11 (UC-003)
- **문제:**
  "ADMIN 계정이 /dashboard 접근 시: 정책 결정: /admin 리다이렉트 또는 ADMIN도 본인 기록 입력 허용"
  이것이 단순한 UX 결정처럼 보이지만, 실제로는 다음과 같은 연쇄 결정을 촉발한다:
  - ADMIN도 WorkRecord를 가질 수 있는가? → 데이터 의미가 달라짐
  - ADMIN이 사장님이면서 동시에 현장 직원이라면 두 역할을 어떻게 처리하는가?
  - /record/:date 에서 ADMIN이 자기 기록을 입력하면 어떤 권한으로 처리되는가?
  이 결정이 없으면 미들웨어(middleware.ts)의 경로 보호 로직을 작성할 수 없다.
- **영향:**
  미들웨어 설계 불완전 → 보안 취약점 가능성. 구현 단계에서 구현자가 임의로 결정하게 됨.
- **개선 방향:**
  UC-003을 명확한 결정사항으로 전환해야 한다. 사장님이 현장 직원 겸 관리자인지 여부를 사용자에게 확인하고 결론을 기록해야 한다.

---

### 9. 달력 날짜 상태 색상이 "제안"이지 "확정"이 아님 — MAJOR

- **위치:** RESEARCH.md 섹션 3.3 (날짜 상태별 색상 제안)
- **문제:**
  색상 정의가 "제안(암묵적 요구)"으로 표시되어 있고 실제 hex 코드나 Tailwind 클래스명이 없다.
  예를 들어 "녹색 계열", "파란색 계열"이라는 표현은 구현자마다 다르게 해석된다.
  달력 셀에서 여러 상태가 동시에 표시될 수 있는가 (공휴일 + 근무)에 대한 정의도 없다.
  미기록 날짜와 주말(토/일)의 시각적 구분 정의도 없다.
  오늘 날짜 강조 스타일(테두리? 배경색?)이 다른 상태 색상과 겹칠 때 우선순위 규칙이 없다.
- **영향:**
  UI 구현 결과가 사용자 기대와 다를 가능성. 특히 색각 이상 사용자 고려 여부, 모바일에서 색상 가독성 등이 전혀 언급되지 않음.
- **개선 방향:**
  색상은 Tailwind CSS 클래스명 수준으로 구체화해야 한다. 상태별 배경색 + 텍스트색 쌍을 테이블로 정의하고, 중첩 상태 우선순위 규칙을 명시해야 한다.

---

### 10. 총 근무시간 계산 단위 미확정이 PDF와 집계에 모두 영향 — MAJOR

- **위치:** RESEARCH.md 섹션 11 (UC-012), 섹션 3.5 (집계 항목 정의), 섹션 3.6 (PDF)
- **문제:**
  UC-012: "총 근무시간 계산 단위 — 소수점 시간(예: 8.5시간) 또는 시간:분 형식"이 미확정이다.
  이것이 미확정인 채로 남아 있으면:
  - WorkRecord.totalHours를 Float으로 저장할지, "HH:mm" String으로 저장할지 달라짐
  - 월간 합산 시 Float 덧셈(8.5 + 7.5 = 16.0) vs 시간:분 합산(08:30 + 07:30 = 16:00) 로직이 달라짐
  - PDF에 "총 근무시간: 162.5시간" vs "총 근무시간: 162시간 30분" 출력 방식이 달라짐
  - 관리자 대시보드 집계 테이블의 컬럼 값 표시 형식이 달라짐
  totalHours를 Float으로 정의하면서(섹션 5.2) UC-012는 미확정이라는 것은 모순이다.
- **영향:**
  데이터 모델, API 응답 형식, UI 표시 형식, PDF 출력 형식 전체에 영향. 구현 후 변경 시 모든 레이어를 수정해야 한다.
- **개선 방향:**
  totalHours를 Float(소수점 시간)으로 DB에 저장하고, 표시 시에는 "X시간 Y분" 또는 "X.Y시간"으로 변환하는 방식을 결정해야 한다. 이 결정을 RESEARCH.md에 명시해야 한다.

---

### 11. 인덱스 전략이 사실상 없음 — MAJOR

- **위치:** RESEARCH.md 섹션 4.1 (성능), 섹션 5.2 (데이터 모델)
- **문제:**
  "DB 인덱스: userId + date 복합 인덱스 필수 (주요 쿼리 패턴)"라는 한 줄만 있다.
  그러나 실제 주요 쿼리 패턴을 분석하면 더 많은 인덱스가 필요하다:
  - 관리자 기간 조회: `WHERE date BETWEEN startDate AND endDate` — date 단독 인덱스도 필요
  - 직원 달력 조회: `WHERE userId = X AND date >= ... AND date <= ...` — 복합 인덱스 (userId, date) OK
  - 비활성 직원 필터: `WHERE isActive = true` — User.isActive 인덱스
  - PDF 생성 시 전 직원 기간 조회: date 범위 + 전 직원 — date 단독 인덱스
  또한 `(userId, date) UNIQUE 제약`이 자동으로 복합 인덱스를 생성하는지 PostgreSQL 동작 확인이 필요하다는 주석이 없다.
- **영향:**
  소규모(5명)이므로 현재는 문제 없지만, 쿼리 패턴이 잘못 설계되면 수천 건에서도 느려질 수 있다.
- **개선 방향:**
  주요 쿼리 패턴별 인덱스 요구사항을 테이블로 명시하고, Prisma 스키마에서 `@@index` 선언 방향을 제시해야 한다.

---

### 12. 배포/운영 — DB 백업 전략이 "권장"으로만 처리됨 — MAJOR

- **위치:** RESEARCH.md 섹션 4.3 (배포/운영), 섹션 10 (RS-007)
- **문제:**
  "PostgreSQL 정기 백업 설정 권장"이라고만 되어 있고 구체적인 전략이 없다.
  노무 신고·세무 처리용 증빙 자료를 저장하는 시스템에서 DB 백업은 선택사항이 아니다.
  다음 내용이 완전히 누락되어 있다:
  - 백업 주기 (일별? 주별?)
  - 백업 보관 기간 (최근 30일? 1년?)
  - 백업 위치 (로컬 디스크? 외부 스토리지? 동일 서버에 백업하면 서버 고장 시 백업도 손실)
  - 백업 복원 절차 (누가, 어떻게)
  - pg_dump 명령어 또는 cron 설정 방향
- **영향:**
  서버 디스크 고장, 실수 데이터 삭제, 해킹 등의 상황에서 복구 불가. 법적 증빙 자료 보존 의무 위반 가능성.
- **개선 방향:**
  최소한 일별 pg_dump + 외부 위치(USB 또는 NAS) 복사 정책을 명시해야 한다. 이것은 "권장"이 아니라 "필수"로 등급을 올려야 한다.

---

### 13. 세션 만료 시 입력 데이터 유실 처리가 "검토"로만 방치됨 — MAJOR

- **위치:** RESEARCH.md 섹션 9 (EC-001), 섹션 12 (4단계 Nice-to-Have)
- **문제:**
  EC-001에서 "LocalStorage 임시 저장 검토"라고만 되어 있고 구현 단계에서는 4단계 Nice-to-Have로 분류되어 있다.
  소방 현장 직원이 업무 내용을 길게 입력하다 세션이 만료되면 모든 내용이 사라진다.
  현장 작업자 특성상 입력 시간이 길 수 있고, 모바일에서는 더 자주 발생할 수 있다.
  LocalStorage에 민감한 업무 기록(근무지, 업무 내용)을 저장하는 것의 보안 리스크도 전혀 검토되지 않았다.
- **영향:**
  사용자 이탈 원인이 될 수 있다. 특히 모바일 현장 입력 시나리오에서 치명적.
- **개선 방향:**
  세션 타임아웃 시간을 구체적으로 설정(예: 8시간)하고, 업무 시간 동안 세션이 만료되지 않도록 연장 전략을 먼저 검토해야 한다. LocalStorage 저장은 보안 검토 후 결정해야 한다.

---

### 14. 직원 등록 시 초기 비밀번호 전달 방법 완전 누락 — MAJOR

- **위치:** RESEARCH.md 섹션 3.7 (직원 계정 관리), 섹션 6.6 (직원 관리 페이지)
- **문제:**
  "직원 등록: 이름, 이메일, 초기 비밀번호 설정"이라고만 되어 있고, 초기 비밀번호를 직원에게 어떻게 전달하는지 방법이 전혀 없다.
  선택지는 다음과 같다:
  (a) 관리자가 구두/문자로 직접 전달 (이메일 발송 기능 없음)
  (b) 시스템이 임시 비밀번호를 화면에 표시 (등록 직후 1회만)
  (c) 이메일 발송 기능 추가
  이메일 발송 기능이 없는 시스템이라면 (b) 또는 (a)인데, 이것이 명시되지 않았다.
  또한 임시 비밀번호를 받은 직원이 처음 로그인 시 비밀번호를 강제 변경해야 하는지 여부도 없다.
- **영향:**
  운영 단계에서 "어떻게 직원한테 비밀번호를 알려주나"라는 실용적 문제가 발생한다.
- **개선 방향:**
  이메일 발송 없이 운영한다면, 등록 직후 임시 비밀번호를 화면에 1회 표시하고 "반드시 복사하세요" 안내를 하는 방식으로 명세해야 한다.

---

### 15. 모바일 반응형 요구사항이 "미확정"으로 방치됨 — MAJOR

- **위치:** RESEARCH.md 섹션 2.1, 섹션 10 (UC-010), 섹션 12 (3단계)
- **문제:**
  UC-010: "모바일 반응형 지원 범위 — 미확정"
  섹션 2.1에서 "현장에서 스마트폰으로 입력 가능성"을 언급하면서도 반응형 지원 범위를 미확정으로 남겼다.
  최소한 다음이 정의되어야 한다:
  - 지원 대상 화면 크기 (최소 375px? 320px?)
  - 달력 뷰의 모바일 레이아웃 (7열 달력이 375px에서 어떻게 보이는가)
  - 모달 vs 별도 페이지 이동 방식 (모바일에서 모달이 불편할 수 있음)
  달력 7열 레이아웃은 모바일에서 날짜 셀이 매우 작아져 클릭하기 어렵다는 UX 문제가 있으며, 이를 어떻게 해결할지 방향이 전혀 없다.
- **영향:**
  현장 직원의 핵심 사용 시나리오인 모바일 입력이 실제로는 쓰기 어려운 UI가 될 수 있다.
- **개선 방향:**
  모바일 최소 지원 해상도와 달력 모바일 레이아웃 방향(예: 주 단위 뷰로 전환 또는 셀 크기 확대)을 명시해야 한다.

---

### 16. 로그 관리 전략 전무 — MINOR

- **위치:** RESEARCH.md 섹션 4.3 (배포/운영)
- **문제:**
  "PM2 logs / pm2 monit"이라고만 언급하고, 다음이 전혀 없다:
  - 로그 보관 기간
  - 로그 로테이션 (pm2-logrotate 사용 여부)
  - 애플리케이션 레벨 오류 로그 (Next.js 서버 에러)
  - 접근 로그 (Nginx access log)
  문제 발생 시 "무엇을 보면 되는가"에 대한 안내가 전혀 없다.
- **영향:**
  운영 중 문제 발생 시 원인 추적이 어렵다.
- **개선 방향:**
  pm2-logrotate 설정, Nginx 로그 위치, Next.js 에러 로그 확인 방법을 간략히 명시해야 한다.

---

### 17. HTTPS 설정이 "권장"으로만 처리됨 — MINOR

- **위치:** RESEARCH.md 섹션 4.2 (보안)
- **문제:**
  "Nginx에서 SSL 처리 권장 (Let's Encrypt)"이라고 되어 있다.
  그러나 HTTPS 없이 운영하면 이메일/비밀번호가 평문으로 전송된다.
  인터넷에 노출된 서버라면 HTTPS는 권장이 아닌 필수다.
  제약사항에서 "도메인/SSL 설정 필요 여부 확인 필요"라고 되어 있어 인터넷 노출 여부 자체가 미확정이다.
  내부망 전용이라면 HTTPS 불필요할 수 있으나, 이 결정이 명시되지 않았다.
- **영향:**
  인터넷 노출 시 자격증명 평문 전송 위험.
- **개선 방향:**
  배포 환경이 인터넷 노출인지 내부망인지를 먼저 확정하고, 인터넷 노출이라면 HTTPS를 필수로 등급 상향해야 한다.

---

### 18. 비활성 직원 기록의 달력 표시 방식 미정 — MINOR

- **위치:** RESEARCH.md 섹션 3.5, 섹션 9 (EC-006), 섹션 11 (UC-006)
- **문제:**
  관리자 대시보드에서 비활성 직원(퇴직자)의 기록을 기간 조회 시 포함할지 여부가 UC-006으로 미확정이다.
  그런데 섹션 3.5의 "활성 직원 필터: 비활성 직원 제외 옵션"은 기본 제외를 시사한다.
  BR-018에서도 "비활성 직원 기록 — 포함 여부 정책 결정 필요"로만 남아 있다.
  이것이 미결정되면 PDF 생성 API의 WHERE 조건, 집계 쿼리, 관리자 UI 필터 옵션 등이 모두 불확실하다.
- **영향:**
  퇴직 직원의 과거 기록이 갑자기 집계에서 사라지면 세무 처리 오류 발생 가능.
- **개선 방향:**
  기본값을 "포함(기록은 영구 보존)"으로 결정하고, 필터 옵션으로 제외 가능하도록 명시해야 한다.

---

### 19. 미래 날짜 입력 허용 여부가 UC-001로 미확정 — MINOR

- **위치:** RESEARCH.md 섹션 3.4, 섹션 6.4, 섹션 7 (BR-010), 섹션 11 (UC-001)
- **문제:**
  미래 날짜 기록 입력 여부가 "업무 계획 선등록 허용 여부 확인 필요"로 남아 있다.
  소방 업종에서 미래 날짜에 휴가를 미리 등록하는 시나리오는 현실적이다.
  그러나 미래 날짜에 WORK 상태로 근무 시간을 미리 입력하는 것은 근무 기록 위조 가능성이 있다.
  두 시나리오를 구분해서 결정해야 한다: (a) 미래 날짜 SICK/ANNUAL 입력 허용 + WORK 불가, (b) 전면 허용, (c) 전면 불가.
- **영향:**
  서버 유효성 검사 로직과 UI의 미래 날짜 클릭 활성화 여부에 영향.
- **개선 방향:**
  Status별로 미래 날짜 허용 여부를 구분하여 결정해야 한다.

---

### 20. 오류 메시지 한국어 정의가 일부만 됨 — MINOR

- **위치:** RESEARCH.md 섹션 4.4, 섹션 6.2, 섹션 6.3, 섹션 6.4
- **문제:**
  로그인 에러 메시지는 한국어로 명시되어 있으나 ("이메일 또는 비밀번호가 올바르지 않습니다"),
  폼 유효성 검사 메시지 (WORK 상태에서 시작 시각 미입력, 종료 시각 < 시작 시각 등)의 한국어 문구가 정의되지 않았다.
  "에러 메시지: 한국어"라는 원칙만 있고, 구체적인 메시지 문구 목록이 없다.
- **영향:**
  구현자마다 다른 에러 메시지를 사용하게 되며, 일관성 없는 UI가 나올 수 있다.
- **개선 방향:**
  주요 유효성 검사 에러 메시지 목록을 테이블로 정의해야 한다. 특히 WORK 상태 필수 필드 관련 메시지.

---

## 총평

| 항목 | 내용 |
|------|------|
| 발견 문제 수 | CRITICAL 5개 / MAJOR 10개 / MINOR 5개 |
| 현재 완성도 평가 | 55% |
| 주요 보완 영역 (우선순위 순) | 야간 근무 데이터 모델, HOLIDAY 테이블 설계, 미확정 비즈니스 규칙 결론 도출, PDF 레이아웃 상세화, 보안 정책 강화 |

### 핵심 지적 사항 요약

RESEARCH.md에서 가장 심각한 문제는 **"미확정(UC-XXX)"으로 방치된 항목들이 실제로는 데이터 모델과 API 설계를 직접 바꾸는 결정 사항**임에도 불구하고, 마치 나중에 결정해도 되는 UX 세부사항처럼 취급되고 있다는 점이다.

특히 다음 5가지는 PLAN.md 작성 전에 반드시 결론이 나야 한다:

1. **야간 근무 처리 방식** (UC-004) — 데이터 모델 변경 여부 결정
2. **HOLIDAY 저장 구조** (UC-007) — 별도 테이블 필요 여부 결정
3. **ADMIN의 직원 기록 수정/삭제 권한** (UC-002) — API 설계 직접 영향
4. **PDF 레이아웃 상세 정의** — 구현 가능 수준의 명세 필요
5. **총 근무시간 단위** (UC-012) — 데이터 모델(Float vs String) 결정

RESEARCH.md는 요구사항 발굴과 문제 제기는 잘 되어 있으나, **결론 없이 나열만 된 미확정 항목이 너무 많다.** PLAN.md 작성 전에 이 항목들에 대한 결론이 나와야 설계가 가능하다.

---

*검토 완료: 2026-05-18*
*다음 단계: RESEARCH.md V2 보완 (researcher 에이전트) 또는 미확정 항목 사용자 확인 후 PLAN.md 작성*
